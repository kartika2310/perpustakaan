! function(e) {
    e.extend({
        uploadPreview: function(l) {
            var i = e.extend({
                input_field: ".dokumen-input",
                preview_box: ".dokumen-preview",
                label_field: ".dokumen-label",
                label_default: "Pilih File",
                label_selected: "Ganti File",
                no_label: !1,
                success_callback: null
            }, l);
            return window.File && window.FileList && window.FileReader ? void(void 0 !== e(i.input_field) && null !== e(i.input_field) && e(i.input_field).change(function() {
                var l = this.files;
                if (l.length > 0) {
                    var a = l[0],
                        o = new FileReader;
                    o.addEventListener("load", function(l) {
                        var o = l.target;
                        if(a.type.match("image")){
                            e(i.preview_box).css("background-image", "url(" + o.result + ")");
                            e(i.preview_box).css("background-size", "cover");
                            e(i.preview_box).css("background-position", "center center");
                        }else{
                            $('#dokumen_files').html('<i class="zmdi zmdi-file-text"></i> Dokumen')
                            e(i.preview_box).css("background-image", "none");
                            e(i.preview_box).css("background-size", "cover");
                            e(i.preview_box).css("background-position", "center center");
                        }
                    }), 0 == i.no_label && e(i.label_field).html(i.label_selected), o.readAsDataURL(a), i.success_callback && i.success_callback()
                } else 0 == i.no_label && e(i.label_field).html(i.label_default), e(i.preview_box).css("background-image", "none"), $('#dokumen_files').html('')
            })) : (alert("You need a browser with file reader support, to use this form properly."), !1)
        }
    })
}(jQuery);